// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  experimental: {
    // Remove the proxy configuration from here
  },
  // Add any other Next.js config options here
  // For example:
  // reactStrictMode: true,
  // images: { ... }
};

export default nextConfig;